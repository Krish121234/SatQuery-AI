# Copy this file to .env and fill in your actual key
# NEVER commit .env — it's already in .gitignore

# Gemini API key for Sid's query router and natural language responses
GEMINI_API_KEY=your_gemini_api_key_here 
